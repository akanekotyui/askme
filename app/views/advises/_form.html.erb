<%= form_with(model: advise, local: true) do |form| %>
  <% if advise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(advise.errors.count, "error") %> prohibited this advise from being saved:</h2>

      <ul>
      <% advise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :en %>
    <%= form.text_field :en, id: :advise_en %>
  </div>

  <div class="field">
    <%= form.label :jp %>
    <%= form.text_field :jp, id: :advise_jp %>
  </div>

  <div class="field">
    <%= form.label :zh %>
    <%= form.text_field :zh, id: :advise_zh %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
